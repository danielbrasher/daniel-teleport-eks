# flux-system

`gotk-components.yaml`, `gotk-sync.yaml` and `kustomization.yaml` are written into this dir by `flux bootstrap github` - don't edit by hand

Bootstrap with `components-extra=image-reflector-controller` and `image-automation-controller`